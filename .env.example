# Docker Compose build configuration. Leave blank for local-only mode.
# VITE_* values are public and embedded in the frontend bundle.
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=

# Optional private hosted wrapper URL. Self-hosters normally leave this blank.
VITE_BILLING_URL=
